<h1>Editing book</h1>
<%= form_with model: @book, url:'/books/id', local:true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <%= f.submit 'update Book' %>
<% end %>



<%= link_to "show",book_path(@book) %>
<%= link_to "back" ,books_path %>